# The deploy artifact: built by scripts/build.mjs, never committed.
dist/

# Wrangler / Cloudflare local state
.wrangler/

# Local working dirs
node_modules/
.claude/
tmp/

# What the harnesses leave behind: browser profiles and screenshots. They no
# longer back the quest log up, because they no longer touch it: a run drives
# a log of its own in a temp directory (FOLIO_LOG).
tests/prof-*/
tests/*.png
tests/tmp/
