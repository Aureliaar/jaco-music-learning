# The deploy artifact: built by scripts/build.mjs, never committed.
dist/

# Wrangler / Cloudflare local state
.wrangler/

# Local working dirs
node_modules/
.claude/
tmp/

# What the harnesses leave behind: browser profiles, screenshots, and the
# backups they take of the quest log before driving it.
tests/prof-*/
tests/*.png
tests/quest-log.*backup.json
